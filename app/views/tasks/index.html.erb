<h1>Tasks#index</h1>

<table>
  <thead>
    <tr>
      <th>name</th>
      <th>content</th>
    </tr>
  </thead>
  <tbody>
    <% @tasks.each do |task|%>
      <tr>
        <td><%= task.name %></td>
        <td><%= task.content %></td>
        
        <%# <td><%= link_to "詳細", task %>
        <%# <><%= link_to "編集", edit_task_path(task) %>
        <%# <td><%= link_to "削除", task, method: :delete, data: { confirm: '本当に削除しますか？' } %></td> %> %>
      </tr>
    <% end %>
</table>