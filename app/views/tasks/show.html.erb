<h1>Tasks詳細</h1>
<p>
  <strong>タスク名:</strong> <%= @task.name %>
</p>
<p>
  <strong>ラベル:</strong>
    <% @task.labels.each do |n| %>
      <%= n.title %>
    <% end %>
</p>
<p>
  <strong>詳しい内容:</strong> <%= @task.content %>
</p>
<p>
  <strong>作成日時:</strong> <%= l @task.created_at %>
<p>
<p>
  <strong>優先順位:</strong> <%= l @task.expired_at, format: :short %>
<p>
  <strong>ステータス:</strong> <%= @task.status %>
</p>
<p>
  <strong>優先度:</strong> <%= @task.priority %>
</p>

<%= link_to "戻る", tasks_path %>